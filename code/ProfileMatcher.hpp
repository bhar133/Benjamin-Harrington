// profileMatcher.hpp (Header file)
#ifndef PROFILEMATCHER_HPP
#define PROFILEMATCHER_HPP

#include <vector>
#include <string>
#include "profile.hpp"

void findMatchesByMajor(const std::string& filename, const std::string& targetMajor); 

#endif // PROFILEMATCHER_HPP